# Schema Status

`education-content.schema.json` is a starting contract for discussion and pilot implementation. It is not a final database schema.

Codex must compare it with:

- the constitution;
- current repository conventions;
- existing domain libraries;
- persistence requirements;
- authoring workflow;
- migration constraints.

Changes must preserve the distinctions among objective, task, observation, evidence, claim, and decision.
